# ============================================
# CONFIGURACIÓN DE DESARROLLO - FERRETERÍA API
# ============================================

# --- Base de Datos ---
# URL de conexión a MariaDB (formato: mysql://usuario:contraseña@host:puerto/nombre_db)
DATABASE_URL="mysql://root:tu-password@localhost:3306/ferreteria"

# --- Autenticación ---
# Secreto para firmar tokens JWT (generar con: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))")
JWT_SECRET="tu-secreto-jwt-de-64-caracteres-hexadecimal"

# --- Servidor ---
# Entorno de ejecución (development | production)
NODE_ENV=development

# Puerto en el que correrá el servidor
PORT=3001

# --- Multi-Tenant ---
# Modo de activación de tenants: manual (dev) | email (prod)
TENANT_ACTIVATION_MODE=manual

# Orígenes permitidos para CORS (separados por coma)
# Ejemplos: http://localhost:5173,http://*.localhost:5173
CORS_ORIGINS=http://localhost:5173,http://*.localhost:5173

# Confiar en headers de proxy (true en producción con nginx/apache, false en desarrollo)
TRUST_PROXY=false

# --- Emails (Solo para Producción) ---
# Habilitar envío de correos electrónicos (false en desarrollo, true en producción)
EMAIL_ENABLED=false

# API Key de Resend (solo necesario si EMAIL_ENABLED=true)
# RESEND_API_KEY=tu-api-key-de-resend

# ============================================
# NOTAS IMPORTANTES
# ============================================
# 
# 1. Este proyecto está configurado para DESARROLLO LOCAL
# 2. No se requieren APIs externas (Resend) en desarrollo
# 3. La activación de tenants es MANUAL mediante POST /api/auth/verify
# 4. Para producción, cambiar:
#    - TENANT_ACTIVATION_MODE=email
#    - EMAIL_ENABLED=true
#    - TRUST_PROXY=true
#    - Agregar RESEND_API_KEY
#
# ============================================
